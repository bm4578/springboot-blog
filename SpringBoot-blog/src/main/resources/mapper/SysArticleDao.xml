<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.onlytype.dao.SysArticleDao">

    <resultMap type="xyz.onlytype.entity.SysArticle" id="SysArticleMap">
        <result property="articleId" column="article_id" jdbcType="VARCHAR"/>
        <result property="uid" column="uid" jdbcType="VARCHAR"/>
        <result property="articleTitle" column="article_title" jdbcType="VARCHAR"/>
        <result property="articleList" column="article_list" jdbcType="VARCHAR"/>
        <result property="articleType" column="article_type" jdbcType="VARCHAR"/>
        <result property="articleCreate" column="article_create" jdbcType="TIMESTAMP"/>
        <result property="articleUpdate" column="article_update" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="articleId" useGeneratedKeys="true">
        insert into minio.sys_article(uid, article_title, article_list, article_type, article_create, article_update)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.uid}, #{entity.articleTitle}, #{entity.articleList}, #{entity.articleType}, #{entity.articleCreate}, #{entity.articleUpdate})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="articleId" useGeneratedKeys="true">
        insert into minio.sys_article(uid, article_title, article_list, article_type, article_create, article_update)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.uid}, #{entity.articleTitle}, #{entity.articleList}, #{entity.articleType}, #{entity.articleCreate}, #{entity.articleUpdate})
        </foreach>
        on duplicate key update
         uid = values(uid) , article_title = values(article_title) , article_list = values(article_list) , article_type = values(article_type) , article_create = values(article_create) , article_update = values(article_update)     </insert>

</mapper>

